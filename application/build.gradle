plugins {
    id 'java-conventions'
    id 'java-library'
}

group = 'com.studies.catalog.admin.application'

dependencies {
    implementation(project(":domain"))

    testImplementation(project(path: ':domain', configuration: 'testClasses'))
}